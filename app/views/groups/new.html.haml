.header
  .header-text
    体温記録アプリ
  


.content
  .select-text
    所属している病棟を選択してください。

  .box
    = form_with model: @group, local: true do |f|
      .select-box
        = f.select :name, [["--", 0],["2階病棟", 1],["3階病棟", 2],["4階病棟",3]]
      .check-box
        = f.collection_check_boxes :user_ids, User.where(name: current_user.name), :id, :name
      .submit-btn
        = f.submit "所属する", class: "SettingGroupForm__button"

.footer
  .under-footer
    &copy; RikutoYamada. 2021.